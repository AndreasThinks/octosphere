[project]
name = "octosphere"
version = "0.1.0"
description = "Octopus → AT Proto bridge (PoC)"
requires-python = ">=3.11"
dependencies = [
  "python-fasthtml @ git+https://github.com/AnswerDotAI/fasthtml.git",
  "requests>=2.31.0",
]

[tool.uv]
package = false
